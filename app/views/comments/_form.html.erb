
  <%= form_for([hunt, comment], url: new_category_hunt_comment_path(hunt.id)) do |f| %>
    <div class="field">
      <%= f.label :comment %>
      <%= f.text_field :comment %>
    </div>
    <div class="field">
      <%= f.submit 'Submit'%>
    </div>
  <% end %>
