
  <%= form_for([hunt, comment], url: new_hunt_comment_path(hunt.id)) do |f| %>
    <div class="field">
      <%= f.label :description %>
      <%= f.text_field :description %>
    </div>
    <div class="field">
      <%= f.submit 'Submit'%>
    </div>
  <% end %>
